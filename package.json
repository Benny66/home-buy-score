{
  "license": "MIT",
  "name": "home-buy-score",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "VITE_SINGLE_FILE=true vite build",
    "build:normal": "VITE_SINGLE_FILE=false vite build",
    "deploy": "yarn build && ./copy_dist_to_benny66.sh",
    "deploy:single": "yarn build:single && ./copy_dist_to_benny66.sh",
    "deploy:normal": "yarn build:normal && ./copy_dist_to_benny66.sh"
  },
  "dependencies": {
    "echarts": "^6.0.0",
    "element-plus": "^2.13.0",
    "vue": "^3.5.24",
    "vue-echarts": "^8.0.1",
    "vue-router": "4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "rollup": "^4.24.0",
    "terser": "^5.44.1",
    "vite": "^5.4.11"
  }
}
